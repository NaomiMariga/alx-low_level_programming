#include "main.h"

/**
 * _abs - changes nagatives to positive numbers
 *
 * @n: number to be changed
 *
 * Return: positive n
 */
int _abs(int n)
{
	if (n < 0)
	{
		n *= -1;
		return (n);
	}
	else
	{
		return (n);
	}
}
