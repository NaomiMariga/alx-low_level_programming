#include <stdio.h>

/**
 * main - entry point
 *
 * Return: Always 0
 */

int main(void)
{
	/*Use puts to output to the stdout*/
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
